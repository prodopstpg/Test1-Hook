if(env.BRANCH_NAME == 'master'){
    stage ('build master'){
        build job: 'For-master'
     }
}
else if(env.BRANCH_NAME == 'Dev'){
    stage ('build Dev'){
        build job: 'For-master'
    }
}
        
